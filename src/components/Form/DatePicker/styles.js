import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapperDatePicker:{
    marginTop: 10,
  },
  textFormTitle:{
    fontSize: 16,
    fontWeight: 'bold',
    textAlign: 'center',
    paddingTop: 5,
    paddingBottom: 5,
    marginTop: 20,
    marginBottom: 10,
    color: '#000',
  },
});

export default styles;
