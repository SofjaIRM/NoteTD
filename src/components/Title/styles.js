import { StyleSheet } from 'react-native';

const sytles = StyleSheet.create({
  wrapperTitle: {
    alignItems: 'center',
  },
  title:{
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 15,
  },
});

export default sytles;
